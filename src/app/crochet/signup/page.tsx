import SignupClient from "./SignupClient";

export const metadata = {
  title: "Sign Up | PixieLoops",
  description: "Create your PixieLoops account to start shopping for beautiful handmade crochet products",
};

export default function SignupPage() {
  return <SignupClient />;
}