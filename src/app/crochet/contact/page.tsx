import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact Us | PixieLoops - Get In Touch",
  description: "Have questions or want to place a custom crochet order? Contact PixieLoops today. We'd love to hear from you!",
};

export default function ContactPage() {
  return <ContactClient />;
}