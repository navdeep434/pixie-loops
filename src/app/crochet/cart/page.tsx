import CartClient from "./CartClient";

export const metadata = {
  title: "Shopping Cart | PixieLoops",
  description: "Review your cart and checkout your handmade crochet products",
};

export default function CartPage() {
  return <CartClient />;
}