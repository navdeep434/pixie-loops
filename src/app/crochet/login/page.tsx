import LoginClient from "./LoginClient";

export const metadata = {
  title: "Login | PixieLoops",
  description: "Login to your PixieLoops account to continue shopping for handmade crochet products",
};

export default function LoginPage() {
  return <LoginClient />;
}